class ApplicationController < ActionController::Base
  # before_action :authenticate_user! [:destroy, :create, :new] # ou outro método de autenticação
  # before_action :authorize_destroy, only: [:destroy]
end
